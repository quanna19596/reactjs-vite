export enum ELayoutPath {
  LANDING = '',
  DASHBOARD = 'dashboard'
}

export enum EPagePath {
  PRODUCTS = 'products',
  USERS = 'users',
  SIGN_IN = 'sign-in',
  SIGN_UP = 'sign-up',
  NOT_FOUND = 'not-found'
}

export enum ESpecialPath {
  EMPTY = '',
  ROOT = '/',
  REST = '*'
}
