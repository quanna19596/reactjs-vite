import { createSlice } from '@reduxjs/toolkit';

import initialState from './initial-state';

const {{camelCase groupName}}Slice = createSlice({
  name: 'clientStore/{{camelCase groupName}}',
  initialState,
  reducers: {
    {{camelCase actionName}}: (state, action) => state
  }
});

export default {{camelCase groupName}}Slice;
