import env from '@/env';
import AuthorizedInstance from '@/services/authorized';

const {{pascalCase serviceName}}Service = AuthorizedInstance(env.service.{{camelCase serviceName}}.baseUrl);

export default {{pascalCase serviceName}}Service;
