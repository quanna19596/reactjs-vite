import { Story, Meta } from '@storybook/react';

import { {{pascalCase componentName}}, T{{pascalCase componentName}}Props } from '@/{{componentType}}';

type T{{pascalCase componentName}}StoryProps = T{{pascalCase componentName}}Props;

export default {
  title: '{{pascalCase componentType}}/{{pascalCase componentName}}',
  component: {{pascalCase componentName}},
  args: {},
  argTypes: {}
} as Meta;

const Template: Story<T{{pascalCase componentName}}StoryProps> = ({ ...rest }) => <{{pascalCase componentName}} {...rest} />;

export const Primary = Template.bind({});
