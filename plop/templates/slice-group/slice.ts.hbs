import { createSlice } from '@reduxjs/toolkit';

import { asyncStatusReducers, successHandler } from '@/redux';

import initialState from './initial-state';
import stateReducers from './state-reducers';

export const {{camelCase groupName}}Slice = createSlice({
  name: '{{camelCase serviceName}}/{{camelCase groupName}}',
  initialState,
  reducers: {
    {{camelCase apiName}}Success: (state, action) => successHandler(state, action, { data: action.payload }),
    ...stateReducers
  },
  extraReducers: asyncStatusReducers
});
