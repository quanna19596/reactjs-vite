import { TInitialState } from '@/redux/types';
import { T{{pascalCase apiName}}Response } from '@/services/{{dashCase serviceName}}/{{dashCase groupName}}/{{dashCase apiName}}';

const initialState: {
  {{camelCase apiName}}: TInitialState<T{{pascalCase apiName}}Response>;
} = {
  {{camelCase apiName}}: { data: undefined, isLoading: undefined, error: undefined }
};

export default initialState;
