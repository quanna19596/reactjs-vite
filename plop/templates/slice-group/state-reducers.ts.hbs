import { errorHandler, requestHandler } from '@/redux';
import { T{{pascalCase apiName}}Parameters, T{{pascalCase apiName}}Response } from '@/services/{{dashCase serviceName}}/{{dashCase groupName}}/{{dashCase apiName}}';
import { TResponseError } from '@/services/{{dashCase serviceName}}/types';

const stateReducers = {
  {{camelCase apiName}}Request: requestHandler<T{{pascalCase apiName}}Parameters, T{{pascalCase apiName}}Response, TResponseError>,
  {{camelCase apiName}}Failed: errorHandler<TResponseError>
};

export default stateReducers;
