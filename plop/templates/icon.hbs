import { EColor } from '@/enums';

import { TSvgProps } from './Icon.types';

const {{pascalCase iconName}}: React.FC<TSvgProps> = ({ primaryColor = EColor.BLACK, secondaryColor = EColor.WHITE }) => {
  return (
    <svg>
      <g />
    </svg>
  );
};

export default {{pascalCase iconName}};
