import { all, takeEvery } from 'redux-saga/effects';

import { {{camelCase groupName}}Slice } from '@/redux/slices/{{dashCase serviceName}}';

import { {{camelCase apiName}}Saga } from './{{dashCase groupName}}';

export default function* {{camelCase serviceName}}RootSaga(): Generator {
  yield all([takeEvery({{camelCase groupName}}Slice.actions.{{camelCase apiName}}Request.type, {{camelCase apiName}}Saga)]);
}
