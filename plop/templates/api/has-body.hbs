import {{pascalCase serviceName}}Service from '@/services/{{dashCase serviceName}}/service';
import { TResponseSuccess } from '@/services/{{dashCase serviceName}}/types';
import { TParameters } from '@/services/types';

export type T{{pascalCase apiName}}Paths = {
  {{pathsFormatted}}
};

export type T{{pascalCase apiName}}Queries = {
  {{queriesFormatted}}
};

export type T{{pascalCase apiName}}Body = {
  {{bodyFormatted}}
};

export type T{{pascalCase apiName}}Parameters = TParameters<T{{pascalCase apiName}}Paths, T{{pascalCase apiName}}Queries, T{{pascalCase apiName}}Body>;

export type T{{pascalCase apiName}}Response = TResponseSuccess;

export const {{camelCase apiName}} = async (params: T{{pascalCase apiName}}Parameters): Promise<T{{pascalCase apiName}}Response> => {
  const response = await {{pascalCase serviceName}}Service.{{camelCase method}}(`{{endpointFormatted}}`, params.body, { params: params.queries });
  return response.data;
};
